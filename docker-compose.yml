services:
  pdftoword:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      MAX_UPLOAD_MB: "50"
      GEMINI_MODEL: "${GEMINI_MODEL:-gemini-3.7-flash}"
    restart: unless-stopped
